//getting the first character: use [0]
var firstLetterOfFirstName = "";
var firstName = "Shannen";

firstLetterOfFirstName = firstName[0];

console.log(firstLetterOfFirstName);

var firstLetterOfLastName = "";
var lastName = "Nazareno";

firstLetterOfLastName = lastName[0];
console.log(firstLetterOfLastName);


//getting the last character: use [name.length - 1];

var str_len = "";
var str = "Hello";

str_len = str[str.length - 1];

console.log("\nGetting the last character: " + str_len);

//note: index number starts with 0, not 1