/*
    parseInt() function
        - takes a string then parse (convert) it into integer
        - if it fails to return an integer, it will return a NaN
*/


function convertFunc(str)
{
    return parseInt(str);
}

var output = convertFunc("90");

console.log(output);

